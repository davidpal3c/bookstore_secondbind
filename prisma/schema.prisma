generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("postgresql://yugen09swarup:0*admin09@db-postgresql-1.cbqg8uma4jiw.us-east-2.rds.amazonaws.com:5432/book_inventory")
}

model Book {
  id              Int      @id @default(autoincrement())
  title           String
  author          String
  genre           String
  publicationDate DateTime
  isbn            String   @unique
  createdAt       DateTime @default(now())
}
